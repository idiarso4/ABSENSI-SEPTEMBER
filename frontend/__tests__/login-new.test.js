// Simple login test with minimal dependencies
describe('Login Form', () => {
  test('1 + 1 equals 2', () => {
    expect(1 + 1).toBe(2);
  });

  test('true is true', () => {
    expect(true).toBe(true);
  });
});
