// Simple login test
describe('Login', () => {
  test('should pass a simple test', () => {
    expect(true).toBe(true);
  });

  test('should add 2 + 2 to equal 4', () => {
    expect(2 + 2).toBe(4);
  });
});
